/** @type {import('next').NextConfig} */
const nextConfig = {
  // The template is content-first and ships no bundled images, so nothing here
  // needs remote image config. Add `images.remotePatterns` if you wire real photos.
};

export default nextConfig;
